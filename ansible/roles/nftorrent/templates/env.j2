INDEXDB_ETCD_HOSTS={{ ansible_host }}:2379
IPFS_CLUSTER_PEERNAME: "{{ inventory_hostname }}"
HTTP_TWA_BOT_POLL=0

# Extra environment
{% for param in nftorrent.environment|dict2items %}
{{ param.key }}={{ param.value }}
{% endfor %}
